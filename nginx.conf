server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;

  # Serve SPA at /dashboard
  location /dashboard/ {
    try_files $uri $uri/ /index.html;
  }

  # Optional health check
  location /health {
    return 200 'ok';
  }
}
